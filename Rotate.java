public class Rotate {
    // changes both mat and persp
    // Rotation about z-axis
    // returns altered persp
    public static Point rotateZ(Point[][] mat, Point persp, double theta) {
        return null;
    }
}
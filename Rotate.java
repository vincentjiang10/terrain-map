public class Rotate {
    // Rotation about z-axis (or possibly left/right)
    // returns altered persp
    public static Point rotateZ(Point[][] mat, Point persp, double theta) {
        return null;
    }

    // Rotation about x-axis (or possibly up/down)
    // returns altered persp
    public static Point rotateX(Point[][] mat, Point persp, double theta) {
        return null;
    }
}